apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  creationTimestamp: null
  name: pullrequest
spec:
  pipelineSpec:
    tasks:
    - name: build
      resources: {}
      taskSpec:
        metadata: {}
        results:
        - name: image-tag
          description: version tag of the container image built and pushed by this task
        stepTemplate:
          image: uses:jenkins-x/jx3-pipeline-catalog/tasks/docker/pullrequest.yaml@versionStream
          name: ""
          resources:
            requests:
              cpu: 400m
              memory: 512Mi
          workingDir: /workspace/source
          volumeMounts:
          - mountPath: /temp-docker-config
            name: temp-docker-config
        steps:
        - image: uses:jenkins-x/jx3-pipeline-catalog/tasks/git-clone/git-clone-pr.yaml@versionStream
          name: ""
          resources: {}
        - name: jx-variables
          resources: {}
        - image: jx3mqubebuild.azurecr.io/docker-io/library/golang:1.26.4
          name: build-make-linux
          resources: {}
          script: |
            #!/bin/bash
            source .jx/variables.sh
            make linux
        - name: check-registry
          resources: {}
        - image: gcr.io/kaniko-project/executor:v1.8.0-debug
          name: build-container
          resources: {}
          script: |
            #!/busybox/sh
            source .jx/variables.sh
            cp /tekton/creds-secrets/tekton-container-registry-auth/.dockerconfigjson /kaniko/.docker/config.json
            /kaniko/executor $KANIKO_FLAGS --context=/workspace/source --dockerfile=${DOCKERFILE_PATH:-Dockerfile} \
            --destination=$PUSH_CONTAINER_REGISTRY/$DOCKER_REGISTRY_ORG/$APP_NAME:$VERSION \
            --destination=$PUSH_CONTAINER_REGISTRY/$DOCKER_REGISTRY_ORG/$APP_NAME:latest-preview \
            --no-push --tarPath image.tar
        - image: jx3mqubebuild.azurecr.io/spring-financial-group/container-tools:latest
          name: push
          resources: {}
          script: |
            #!/bin/bash
            source .jx/variables.sh
            cp /temp-docker-config/config.json ~/.docker/config.json
            crane push /workspace/source/image.tar $PUSH_CONTAINER_REGISTRY/$DOCKER_REGISTRY_ORG/$APP_NAME:$VERSION
            crane push /workspace/source/image.tar $PUSH_CONTAINER_REGISTRY/$DOCKER_REGISTRY_ORG/$APP_NAME:latest-preview
            rm /workspace/source/image.tar
            echo -n "$VERSION" > $(results.image-tag.path)
        volumes:
        - name: temp-docker-config
          secret:
            secretName: tekton-temp-registry-auth
            items:
            - key: .dockerconfigjson
              path: config.json
    - name: test-package-generation
      runAfter:
      - build
      resources: {}
      taskSpec:
        metadata: {}
        steps:
        - image: jx3mqubebuild.azurecr.io/spring-financial-group/jx3-openapi-generation:$(tasks.build.results.image-tag)
          name: test-package-generation
          resources: {}
          script: |
            #!/bin/bash
            git config --global user.name ${GIT_AUTHOR_NAME:-mqube-bot}
            git config --global user.email ${GIT_AUTHOR_EMAIL:-mqube-bot@mqube.com}
            jx3-openapi-generation test
  podTemplate: {}
  serviceAccountName: tekton-bot
  timeout: 12h0m0s
status: {}
